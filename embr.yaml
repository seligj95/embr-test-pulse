database:
  enabled: true
  engine: postgres
  framework: prisma
  schema: prisma/schema.prisma
  sync: "npx prisma db push --accept-data-loss --skip-generate"
  seed: "node scripts/seed.mjs"

healthCheck:
  path: /api/health

